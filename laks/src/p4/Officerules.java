package p4;

public interface Officerules {
	//body can't define
	//by default,methods in interface are abstract
	//by default,methods in interface are public
	//by default,variables in interface are final not methods
	//by default, variables in interface are static
	//by default, variables in interface are public
	//private access modifier not allowed in interface 
	//by default can't method final because it is abstract
//in constructor we can't gave final
	//we gave final infront of object
int salary=25000;
int leave=10;
void worl_8_hours();
void wear_formals();
void get_salary();
void apply_leave();
}
